# MetrCheck AI — Cloud / lightweight deployment requirements (Render, Railway, HF Spaces)
# Excludes paddleocr/paddlepaddle (2GB+ RAM) in favor of pytesseract (~200MB)
# For Render free tier (512MB RAM), HF Spaces, Railway, or any low-memory container

# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
starlette==1.6.0

# --- OCR & imaging (cloud: Tesseract only) ---
Pillow==12.3.0
pytesseract>=0.3.10
opencv-python-headless==5.0.0.93   # headless = no libGL dependency on Linux
numpy==2.2.6

# --- Storage / IO ---
aiosqlite==0.22.1
aiofiles==25.1.0

# --- Config & validation ---
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
jinja2==3.1.6

# --- Reports (PDF & Excel) ---
reportlab==5.0.1
openpyxl==3.1.5
qrcode>=8.0

# --- HTTP client ---
httpx==0.28.1

# --- Auth ---
PyJWT>=2.8.0
bcrypt>=4.0.0

# --- Testing (optional, harmless in container) ---
pytest==9.1.1
pytest-asyncio==1.4.0
